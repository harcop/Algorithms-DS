/// LeetCode #777 - Swap Adjacent in LR String (Premium)
pub const NOTE: &str = "LeetCode Premium #777 — L/R/X swap reachability; solve on LeetCode.";

fn main() {
    println!("{}", NOTE);
}

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        assert!(!super::NOTE.is_empty());
    }
}
