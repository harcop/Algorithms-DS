/// LeetCode #742 - Closest Leaf in a Binary Tree (LeetCode Premium)
pub const NOTE: &str = "LeetCode Premium problem — statement and tests live on LeetCode; not duplicated in-repo.";

fn main() {
    println!("{}", NOTE);
}

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        assert!(!super::NOTE.is_empty());
    }
}
