/// LeetCode #1810 - Minimum Path Cost in a Hidden Grid (Premium, interactive)
pub const NOTE: &str = "LeetCode Premium #1810 — GridMaster API; solve on LeetCode.";

fn main() {
    println!("{}", NOTE);
}

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        assert!(!super::NOTE.is_empty());
    }
}
