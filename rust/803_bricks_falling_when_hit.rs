/// LeetCode #803 - Bricks Falling When Hit (Premium)
pub const NOTE: &str = "LeetCode Premium #803 — union-find reverse hits; solve on LeetCode.";

fn main() {
    println!("{}", NOTE);
}

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        assert!(!super::NOTE.is_empty());
    }
}
