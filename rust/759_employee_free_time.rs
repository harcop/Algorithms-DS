/// LeetCode #759 - Employee Free Time (LeetCode Premium)
pub const NOTE: &str = "LeetCode Premium problem — schedule merge / free intervals; not duplicated in-repo.";

fn main() {
    println!("{}", NOTE);
}

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        assert!(!super::NOTE.is_empty());
    }
}
