/// LeetCode #774 - Minimize Max Distance to Gas Station (Premium)
pub const NOTE: &str = "LeetCode Premium #774 — binary search on max distance; solve on LeetCode.";

fn main() {
    println!("{}", NOTE);
}

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        assert!(!super::NOTE.is_empty());
    }
}
