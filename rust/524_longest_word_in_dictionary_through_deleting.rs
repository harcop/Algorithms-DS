/// LeetCode #524 - Longest Word In Dictionary Through Deleting (premium; reference only)
pub const NOTE: &str = "Implementation omitted in this set.";

fn main() {
    println!("{}", NOTE.len());
}

#[cfg(test)]
mod tests {
    use super::NOTE;

    #[test]
    fn note_non_empty() {
        assert!(!NOTE.is_empty());
    }
}
