/// LeetCode #801 - Minimum Swaps To Make Sequences Increasing (Premium)
pub const NOTE: &str = "LeetCode Premium #801 — DP on two sequences; solve on LeetCode.";

fn main() {
    println!("{}", NOTE);
}

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        assert!(!super::NOTE.is_empty());
    }
}
