/// LeetCode #805 - Split Array With Same Average (Premium)
pub const NOTE: &str = "LeetCode Premium #805 — subset average DP; solve on LeetCode.";

fn main() {
    println!("{}", NOTE);
}

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        assert!(!super::NOTE.is_empty());
    }
}
