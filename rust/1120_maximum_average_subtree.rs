/// LeetCode #1120 - Maximum Average Subtree (Premium)
pub const NOTE: &str = "LeetCode Premium #1120 — solve on LeetCode.";

fn main() {
    println!("{}", NOTE);
}

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        assert!(!super::NOTE.is_empty());
    }
}
