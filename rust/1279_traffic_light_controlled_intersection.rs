/// LeetCode #1279 - Traffic Light Controlled Intersection (Premium)
pub const NOTE: &str = "LeetCode Premium #1279 — solve on LeetCode.";

fn main() {
    println!("{}", NOTE);
}

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        assert!(!super::NOTE.is_empty());
    }
}
