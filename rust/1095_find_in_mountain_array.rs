/// LeetCode #1095 - Find in Mountain Array (Premium)
pub const NOTE: &str = "LeetCode Premium #1095 — solve on LeetCode.";

fn main() {
    println!("{}", NOTE);
}

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        assert!(!super::NOTE.is_empty());
    }
}
