/// LeetCode #1096 - Brace Expansion II (Premium)
pub const NOTE: &str = "LeetCode Premium #1096 — solve on LeetCode.";

fn main() {
    println!("{}", NOTE);
}

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        assert!(!super::NOTE.is_empty());
    }
}
