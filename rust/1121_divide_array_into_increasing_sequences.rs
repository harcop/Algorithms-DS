/// LeetCode #1121 - Divide Array Into Increasing Sequences (Premium)
pub const NOTE: &str = "LeetCode Premium #1121 — solve on LeetCode.";

fn main() {
    println!("{}", NOTE);
}

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        assert!(!super::NOTE.is_empty());
    }
}
