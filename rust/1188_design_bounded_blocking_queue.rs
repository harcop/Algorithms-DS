/// LeetCode #1188 - Design Bounded Blocking Queue (Premium)
pub const NOTE: &str = "LeetCode Premium #1188 — solve on LeetCode.";

fn main() {
    println!("{}", NOTE);
}

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        assert!(!super::NOTE.is_empty());
    }
}
