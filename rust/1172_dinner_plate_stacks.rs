/// LeetCode #1172 - Dinner Plate Stacks (Premium)
pub const NOTE: &str = "LeetCode Premium #1172 — solve on LeetCode.";

fn main() {
    println!("{}", NOTE);
}

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        assert!(!super::NOTE.is_empty());
    }
}
